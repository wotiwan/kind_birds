# План разработки игры

## 1. Границы экрана
- Добавить коллижен модели границ экрана на главной сцене: верх, низ, право, лево.
- Добавить еще две сцены с другим бекграундом, туда также добавить границы.

## 2. Разметка моделей
- Нарезать нужные png из `bird_or_enemy.png`, `tiles v1.png` и `blocks2.png`
  - Создать сцены для каждого объекта. Птицы и разрушаемые блоки типа RigidBody2D
  - Выполнять эти пункты по примеру blue_bird

# Клонирование репозитория и начало работы

  1. В любой удобной папке запускаем консоль cmd
  2. указываем команду `git clone https://github.com/wotiwan/kind_birds`
  3. Запускаем godot, выбираем "импортировать проект" (Ctrl + i)
  4. Указываем путь до склонированной папки

# Внесение изменений в репозиторий
## Отправляем изменения также через консоль сmd, используя команды:
  - `git add .`
  - `git commit -m "текст коммита"`
  - `git push -u origin main`